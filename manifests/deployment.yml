apiVersion : apps/v1
kind: Deployment
metadata:
  name: "honey-staking"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "honey-staking"
  template:
    metadata:
      labels:
        app: "honey-staking"
    spec:
      containers:
        - name: "honey-staking"
          image: "honeyland.azurecr.io/honey-staking"
          ports:
          - containerPort: 80