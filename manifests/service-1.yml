apiVersion: v1
kind: Service
metadata:
    name: "honey-bfa1"
    labels:
        app: "honey-bfa1"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "honey-bfa1"