apiVersion: v1
kind: Service
metadata:
    name: "honey-72aa"
    labels:
        app: "honey-72aa"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "honey-72aa"